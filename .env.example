# === Development (two separate processes) ===
# Not needed for local dev — auto-detected (localhost:3001)
# NEXT_PUBLIC_SOCKET_URL=http://localhost:3001

# === Production (single Docker container) ===
# PORT is set by Render automatically. Default: 3000
# PORT=3000
# No NEXT_PUBLIC_SOCKET_URL needed — same origin
