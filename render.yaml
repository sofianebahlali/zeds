services:
  - type: web
    name: quizz-arena
    runtime: node
    buildCommand: echo "=== Listing src ===" && ls -la src/ && npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
