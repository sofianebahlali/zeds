services:
  - type: web
    name: quizz-arena
    runtime: node
    buildCommand: echo "=== tsconfig.json ===" && cat tsconfig.json && npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
